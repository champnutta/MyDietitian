{
  "name": "@mydietitian/backend",
  "private": true,
  "version": "0.1.0",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "firebase emulators:start --only functions",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.1",
    "jose": "^4.15.4"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
